name: billie-platform-crm

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
      args:
        - GITHUB_TOKEN=${GITHUB_TOKEN}
    container_name: billie-platform-crm
    ports:
      - '3000:3000'
    volumes:
      - .:/app
      - node_modules:/app/node_modules
      # Mount only AWS SSO config and token cache (not credentials file)
      - ${HOME}/.aws/config:/home/nextjs/.aws/config:ro
      - ${HOME}/.aws/sso:/home/nextjs/.aws/sso:ro
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app/event-processor/src
      - AWS_CONFIG_FILE=/home/nextjs/.aws/config
    extra_hosts:
      # Allows container to reach host machine's localhost services
      - "host.docker.internal:host-gateway"
    networks:
      - billie-platform-network

volumes:
  node_modules:

networks:
  billie-platform-network:
    name: billie-platform-network
    driver: bridge