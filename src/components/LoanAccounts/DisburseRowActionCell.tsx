'use client'

import React from 'react'

/**
 * Compatibility component for Payload importMap.
 *
 * This component is referenced by the generated admin import map in some local
 * environments. The dedicated disbursement workflow now lives in the custom
 * pending disbursements queue view.
 */
export function DisburseRowActionCell() {
  return null
}

export default DisburseRowActionCell
